{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://codex-nomad-surface.local/docs/protocols/promptform.schema.json",
  "title": "promptform",
  "description": "Assistant-to-UI Prompt Form protocol for embedded response forms in Codex Nomad Surface.",
  "type": "object",
  "additionalProperties": false,
  "required": ["template", "fields"],
  "properties": {
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "UI heading for the rendered form."
    },
    "purpose": {
      "type": "string",
      "description": "Short statement of what the Prompt Form is for."
    },
    "usage": {
      "type": "string",
      "description": "Optional guidance on how the user should use the Prompt Form."
    },
    "response_example": {
      "type": "string",
      "minLength": 1,
      "description": "Recommended example of the plain-text reply the user can send after filling the form."
    },
    "submit_label": {
      "type": "string",
      "minLength": 1,
      "description": "Label for the form submission button."
    },
    "append_spacing": {
      "type": "string",
      "enum": ["none", "line", "paragraph"],
      "description": "Controls how generated text is appended to the current draft."
    },
    "template": {
      "type": "string",
      "minLength": 1,
      "description": "Plain-text reply template used to generate the next draft text. Use {field_id} placeholders."
    },
    "fields": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/field"
      },
      "description": "List of field definitions."
    }
  },
  "$defs": {
    "optionString": {
      "type": "string"
    },
    "optionObject": {
      "type": "object",
      "additionalProperties": false,
      "required": ["value"],
      "properties": {
        "value": {
          "type": "string"
        },
        "label": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "option": {
      "oneOf": [
        {
          "$ref": "#/$defs/optionString"
        },
        {
          "$ref": "#/$defs/optionObject"
        }
      ]
    },
    "fieldBase": {
      "type": "object",
      "additionalProperties": false,
      "required": ["id", "type"],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "type": {
          "type": "string"
        },
        "label": {
          "type": "string",
          "minLength": 1
        },
        "placeholder": {
          "type": "string"
        },
        "help": {
          "type": "string"
        },
        "required": {
          "type": "boolean"
        }
      }
    },
    "radioField": {
      "allOf": [
        {
          "$ref": "#/$defs/fieldBase"
        },
        {
          "type": "object",
          "required": ["options"],
          "properties": {
            "type": {
              "const": "radio"
            },
            "default": {
              "type": "string",
              "description": "Initially selected option value. When omitted, the first option is selected."
            },
            "options": {
              "type": "array",
              "minItems": 1,
              "items": {
                "$ref": "#/$defs/option"
              }
            }
          }
        }
      ]
    },
    "selectField": {
      "allOf": [
        {
          "$ref": "#/$defs/fieldBase"
        },
        {
          "type": "object",
          "required": ["options"],
          "properties": {
            "type": {
              "const": "select"
            },
            "default": {
              "type": "string",
              "description": "Initially selected option value. When omitted, the first option is selected."
            },
            "options": {
              "type": "array",
              "minItems": 1,
              "items": {
                "$ref": "#/$defs/option"
              }
            }
          }
        }
      ]
    },
    "textField": {
      "allOf": [
        {
          "$ref": "#/$defs/fieldBase"
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "text"
            },
            "default": {
              "type": "string"
            }
          }
        }
      ]
    },
    "textareaField": {
      "allOf": [
        {
          "$ref": "#/$defs/fieldBase"
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "textarea"
            },
            "default": {
              "type": "string"
            }
          }
        }
      ]
    },
    "checkboxField": {
      "allOf": [
        {
          "$ref": "#/$defs/fieldBase"
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "checkbox"
            },
            "default": {
              "type": "boolean"
            },
            "checked_value": {
              "type": "string"
            },
            "unchecked_value": {
              "type": "string"
            }
          }
        }
      ]
    },
    "field": {
      "oneOf": [
        {
          "$ref": "#/$defs/radioField"
        },
        {
          "$ref": "#/$defs/selectField"
        },
        {
          "$ref": "#/$defs/textField"
        },
        {
          "$ref": "#/$defs/textareaField"
        },
        {
          "$ref": "#/$defs/checkboxField"
        }
      ]
    }
  }
}
